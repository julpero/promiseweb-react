
export enum RULES {
  noEvenPromisesAllowed,
  hiddenPromiseRound,
  onlyTotalPromise,
  mustPlayTrump,
  hiddenTrump,
  speedPromise,
  privateSpeedGame,
  opponentPromiseCardValue,
  opponentGameCardValue,
}
