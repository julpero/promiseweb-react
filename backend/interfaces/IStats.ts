export interface IRoundOfPlayerStats {
  game: string,
  playersInGame: number,
  played: number,
  round: number,
  cardsInRound: number,
  name: string,
  promise: number,
  keeps: number,
  points: number,
  kept: boolean,
  promiseTime: number,
  playTime: number,
}
